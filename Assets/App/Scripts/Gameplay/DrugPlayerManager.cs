using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DrugPlayerManager : MonoBehaviour
{
    public bool isInDrugEffect;
    public Drug[] DrugList;
    // public static DrugPlayerManager instance;

    private void Awake()
    {
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
